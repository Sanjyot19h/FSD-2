In this experiment, we learned form handling and validation using Material UI.

=useState stores input values and error messages.

=validate() checks email format and password length.

=TextField is used for inputs with built-in error styling.

=helperText displays validation messages.
